# Production (Vercel production deployment)
# Used when NODE_ENV=production; set real values in Vercel dashboard

# MongoDB – production Atlas cluster (set in Vercel → Environment Variables → Production)
# Example (Atlas): mongodb+srv://prod-user:password@cluster0.mongodb.net/mis
MONGODB_URI=mongodb+srv://prod-user:password@cluster0.mongodb.net/mis

# NextAuth – set in Vercel (e.g. openssl rand -base64 32)
NEXTAUTH_SECRET=your-production-secret

# Production app URL (e.g. custom domain or Vercel alias)
NEXTAUTH_URL=https://mis-ruby-two.vercel.app
