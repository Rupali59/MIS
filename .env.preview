# Preview (Vercel preview deployments: PRs, branch deploys)
# Vercel sets VERCEL_ENV=preview; use Preview env vars in dashboard

# MongoDB – use a dedicated preview DB or same as dev (e.g. Atlas cluster)
# Example (Atlas): mongodb+srv://preview-user:password@cluster0.mongodb.net/mis
MONGODB_URI=mongodb+srv://preview-user:password@cluster0.mongodb.net/mis

# NextAuth – set real value in Vercel → Project → Settings → Environment Variables (Preview)
NEXTAUTH_SECRET=your-preview-secret

# Preview URL – overridden per-deployment in Vercel (Preview env),
# typically a preview URL like https://mis-<hash>-tathya-59.vercel.app
NEXTAUTH_URL=https://mis-preview.example.com
