# Development (local): NODE_ENV=development
# Used when running: npm run dev

# MongoDB – local or dev Atlas cluster
MONGODB_URI=mongodb://localhost:27017/mis

# NextAuth – set NEXTAUTH_SECRET in .env.development.local (never commit)
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=your-development-secret

# App URL (local dev on port 9006)
NEXTAUTH_URL=http://localhost:9006
